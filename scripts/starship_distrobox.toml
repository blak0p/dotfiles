# Hereda toda la configuración visual y colores del tema de Gentleman
inherit_from = ["/home/alejandro/.config/starship.toml"]

# Modificamos el formato principal para anteponer una caja estilizada en el tema de Gentleman (󰏖)
format = """[󰏖 ](bold fg:peach)($directory)$os$git_branch$fill$nodejs$rust$golang$php$bun$java$c$conda$zig$cmd_duration$time
$character"""
